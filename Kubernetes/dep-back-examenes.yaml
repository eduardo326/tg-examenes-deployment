apiVersion: apps/v1
kind: Deployment
metadata:
  annotations:
    kubernetes.io/change-cause: "Despliegue de componente ms-examenes"
  name: deployment-ms-examenes
  namespace: prod-back
  labels:
    app: dep-ms-examenes
spec:
  replicas: 1
  selector:
    matchLabels:
      app: tg-ms-examenes
  template:
    metadata:
      labels:
        app: tg-ms-examenes
    spec:
      containers:
      - name: ms-examenes
        image: gardinsoft/tg-examenes:v1.0.2
        resources:
          limits:
            memory: "300Mi"
            cpu: "0.3"
          requests:
            memory: "100Mi"
            cpu: "0.1"
        readinessProbe:
          tcpSocket:
            port: 8002
          initialDelaySeconds: 60
          periodSeconds: 30
        livenessProbe:
          tcpSocket:
            port: 8002
          initialDelaySeconds: 60
          periodSeconds: 30